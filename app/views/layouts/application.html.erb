<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>It's Ready</title>

  <link rel="shortcut icon" href="/favicon.ico">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">

  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
</head>

<body>
  <div id="main-nav" class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
      <% if (controller.class == UsersController && controller.action_name == 'new') || controller.class == HomeController || controller.class == SessionsController %>
        <a class="brand" href="/">It's Ready</a>
      <% else %>
        <a class="brand" href="/"><%= image_tag 'fv_logo.png', style: 'height: 30px; width: 76px; position: relative; left: 10px; top: -4px'%></a>
      <% end %>
      <%= yield :navbar %>
    </div>
  </div>

  <div id="main" class="row-fluid">
    <div id="main-inner" class="offset1 span10">
      <%= render partial: 'shared/flash_messages' %>
      <%= yield %>
    </div>
  </div>

  <script>
    <%= yield :js %>
  </script>
</body>

</html>
