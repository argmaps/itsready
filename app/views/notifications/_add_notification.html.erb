<%= simple_form_for([current_user, current_user.notifications.new]) do |f| %>
      <%= hidden_field_tag('notification[message]', "It's ready.") -%>
      <span id="add-notification-marker">Notify</span>
      <%= collection_select(:notification, :customer_id, current_user.customers, :id, :full_name) %>
<% end %>
